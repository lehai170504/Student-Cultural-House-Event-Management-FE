import { combineReducers } from '@reduxjs/toolkit';
// import userReducer from '@/features/users/slices/userSlice'; // Commented out: users feature doesn't exist

const rootReducer = combineReducers({
  // user: userReducer, // Commented out: users feature doesn't exist
  // Add other reducers here as needed
});

export default rootReducer;
